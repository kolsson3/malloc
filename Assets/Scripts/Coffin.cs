using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coffin : MonoBehaviour
{
    GameObject player;

    public bool hasCandle = false;
    public bool hasPhoto = false;
    public bool hasFlowers = false;

    public bool placedCandle = false;
    public bool placedPhoto = false;
    public bool placedFlowers = false;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
